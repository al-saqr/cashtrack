import 'package:flutter/material.dart';

class DailyTabPage extends StatelessWidget {
  const DailyTabPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text('DAILY');
  }
}
