import 'package:flutter/material.dart';

class WeeklyTabPage extends StatelessWidget {
  const WeeklyTabPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('WEEKLY'));
  }
}
